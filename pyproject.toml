[project]
name = "acs-auto-sit"
version = "0.1.0"
description = "Local ACS automated SIT tool for 3DS browser AReq and CReq flows"
requires-python = ">=3.12"
dependencies = [
  "openpyxl>=3.1",
]

[tool.pytest.ini_options]
pythonpath = ["."]
addopts = "-q -p no:cacheprovider"
